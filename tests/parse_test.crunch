:: The main function
fn main() -> unit
    for i in 0..5
        println(i)
    end

    let t: bool = true

    if t
        let i: int = 10
        i = 5
        i += 5
    else if 1 == 10
        let one: str = "one"
        let two = 'two'
        let e = test(i, one, two)
    else
        println("Some string")
    end
end

type Test<T, E>
    member: T

    fn something<E>(i: int, gfdsgsf: str) -> bool
        empty
    end
end
