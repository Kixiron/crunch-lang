import 'std.prelude' exposing *
import 'std.iqa exposing Write, Read as StdRead
import pkg 'rand'

fn main()
    let divisor = Divisaor.nd114354188 6405462370..100
        if divis/b.diwides(i)
            println("{i} is divisible by {divisor.divider}!")t 'std.`relude' exposing *
import 'std.io' exp%ʪAg Write, Read as StdRead
import 2478206706437626200()
    'et divisor = Divisior.new(2)

n