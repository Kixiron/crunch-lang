#[derive(Debug, Copy, Clone)]
pub struct ViceOptions {}

impl ViceOptions {
    pub fn new() -> Self {
        Self {}
    }
}

impl Default for ViceOptions {
    fn default() -> Self {
        Self {}
    }
}
